<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark" id="navbar">
    <a class="navbar-brand" href="#">DesoBot</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item" id="nav-home-link">
                <a class="nav-link" href="#">Home</a>
            </li>
            <li class="nav-item" id="nav-help-link">
                <a class="nav-link" href="#help">Help</a>
            </li>
        </ul>
        {{#if whoami.authenticated}}
        <ul class="navbar-nav">
            <li class="nav-item dropdown">
                <a id="logindropdown" class="nav-link dropdown-toggle" href="#" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{whoami.username}}#{{whoami.discriminator}}
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="logindropdown">
                    <img class="dropdown-header rounded-circle mx-auto d-block" src="{{whoami.avatarUrl}}" />
                    <div class="dropdown-header">Logged in as {{whoami.username}}#{{whoami.discriminator}}</div>
                    <div class="dropdown-divider"></div>
                    <a class="btn btn-outline-success my-2 my-sm-0 dropdown-item" href="/api/Auth/SignOut">Sign Out</a>
                </div>
            </li>
        </ul>
        {{else}}
        <a class="btn btn-outline-success my-2 my-sm-0" href="/api/Auth/SignIn">
            Log in with Discord
        </a>
        {{/if}}
    </div>
</nav>

{{#if botInfo.testing}}
<div class="alert alert-warning container text-center" role="alert">
    <h4><b>Warning!</b> This is a test instance of DesoBot!</h4>
    (Need to disable this warning? Compile in release mode!)
</div>
{{/if}}